{
  "name": "link-station",
  "version": "1.0.0",
  "description": "Link Station - Real-time matching game",
  "main": "api/game.js",
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.x"
  }
}


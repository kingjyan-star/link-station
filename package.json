{
  "name": "link-station",
  "version": "3.0.5",
  "description": "Link Station - Offline Party Mini-Game Platform (오프라인 술자리 미니게임 플랫폼)",
  "main": "api/game.js",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "test:liar": "node scripts/test-liar-game.js",
    "test:tab-close": "node scripts/test-tab-close-reclaim.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.x"
  }
}

